// ---------------------------------------------------------------------------
// INERT STUB — Supabase-backed Properties/Loans reads & writes.
// The real implementation is not present in this repo. With enabled=false,
// server.js falls back to its ClickUp code paths (which themselves need a
// CLICKUP_API_TOKEN). This stub only exists so server.js can `require()` it
// and boot. Replace with the real module to activate Supabase-backed Properties.
// ---------------------------------------------------------------------------
module.exports = {
  enabled: false,
};
